package org.tcs.webdriver;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public interface FrameHelper {
    Logger LOGGER = LogManager.getLogger(FrameHelper.class);

    //Work in progress
}
